 console.log("Hello! I'm your coding fun fact guide!");

// إنشاء المتغيرات الثلاثة
let botName = "CodeBot";
let botLocation = "the Cloud";
let favoriteLanguage = "JavaScript";

// تسجيل الاسم والمكان
console.log("My name is " + botName + " and I live on " + botLocation + ".");

// تسجيل اللغة المفضلة
console.log("My favorite programming language is " + favoriteLanguage + ".");

// إنشاء codingFact وتعيين أول حقيقة
let codingFact = "Did you know that " + favoriteLanguage + " was created in just 10 days?";
console.log(codingFact);

// إعادة تعيين codingFact إلى حقيقة ثانية
codingFact = "One of the most popular frameworks for " + favoriteLanguage + " is React.";
console.log(codingFact);

// إعادة تعيين codingFact إلى حقيقة ثالثة
codingFact = favoriteLanguage + " uses prototypal inheritance instead of classical inheritance.";
console.log(codingFact);

// رسالة الوداع
console.log("It was fun sharing these facts with you. Goodbye! - " + botName + " from " + botLocation + ".");

** end of script.js **
